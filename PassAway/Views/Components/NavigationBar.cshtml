@model IEnumerable<PassAway.Models.ViewModels.ElementViewModel>

<nav class="">
    <ul class=" ">
        @foreach (var item in Model) {
            <li>
                <a href="@item.URL">@item.Name</a>
            </li>
        }
    </ul>
</nav>
