@model IEnumerable<ElementViewModel>

<nav class="">
    <ul class=" ">
        @foreach (var item in Model) {
            <li>
                <a href="@item.URL">@item.Name</a>
            </li>
        }
    </ul>
</nav>
